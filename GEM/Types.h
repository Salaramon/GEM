#pragma once
#include <array>
#include <string>

namespace gem {
	std::array<std::string, 2> includeSpecifiers = { "set", "add" };
	std::array<std::string, 4> macroSpecifiers = { "bool", "int", "float", "double"};

}